{
  "name": "fontend",
  "version": "1.0.0",
  "description": "Proyecto de prueba assesment front",
  "main": "index.js",
  "scripts": {
    "test": "cypress open",
    "ui-smoke-allure": "cypress run --browser chrome --spec cypress/e2e/**.js --env allure=true",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "htmlreport": "cypress run --spec cypress/e2e/**.js --browser chrome --headed"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "@shelex/cypress-allure-plugin": "^2.40.1",
    "cypress": "^13.6.4",
    "cypress-mochawesome-reporter": "^3.8.1",
    "cypress-wait-until": "^3.0.1",
    "cypress-xpath": "^2.0.1",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.0.1"
  }
}
